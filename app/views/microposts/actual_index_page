<% if user_signed_in? %>
<% provide(:title, "ALL POSTS ") %>
<% provide(:name, "listing microposts") %>
 <table class="table ">
  <thead>
    <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
</thead>
  
 
  <% @microposts.each do |micropost| %>
   
    <li id="micropost-<%= micropost.id %>">
  <span class="user"><%=  current_user.name %></span>
  <span class="content"><%= micropost.title %></span>
  <span class="content"><%= micropost.text %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
  </span>
</li>
  <% end %>


<%= link_to 'New post', new_micropost_path %>
<% else %>
log-in page SHOULD BE RENDERED
<% end %>